a=(11,12,13,14,15,15)
print((a))
 
no = a.count(15)
print(no)
#print a no of the string 
# Here are the **most commonly used Tuple methods and operations in Python**:

# ---

# ### **Tuple Methods**

# Tuples are **immutable**, so they have very few methods compared to lists.just i love ass

# | Method         | Description                           | Example                       |
# | -------------- | ------------------------------------- | ----------------------------- |
# | `count(value)` | Returns number of times value appears | `(1, 2, 3, 2).count(2)` → `2` |
# | `index(value)` | Returns first index of the value      | `(1, 2, 3).index(2)` → `1`    |

# ---

# ### **Common Tuple Operations**

# Even though tuples are immutable, we can still perform these:

# 1. **Create a tuple**

# ```python
# t = (1, 2, 3)
# ```

# 2. **Access elements**

# ```python
# print(t[0])    # 1
# print(t[-1])   # 3
# ```

# 3. **Slicing**

# ```python
# print(t[0:2])  # (1, 2)
# ```

# 4. **Concatenation**

# ```python
# t1 = (1, 2)
# t2 = (3, 4)
# print(t1 + t2)  # (1, 2, 3, 4)
# ```

# 5. **Repetition**

# ```python
# print(t1 * 3)   # (1, 2, 1, 2, 1, 2)
# ```

# 6. **Check existence**

# ```python
# print(2 in t1)  # True
# ```incci    d vcigf vcvfio koiliep 

# 7. **Length**

# ```python
# print(len(t1))  # 2
# ```

# 8. **Convert to list**

# ```python
# lst = list(t1)
# ```

# 9. **Nested Tuples**

# ```python
# nested = ((1, 2), (3, 4))
# print(nested[1][0])  # 3
# ```

# ---

# **Special: Tuple Packing & Unpacking**

# ```python
# a, b, c = (1, 2, 3)
# print(a, b, c)  # 1 2 3



#  Make a **PDF cheat sheet** for all **tuple methods + operations with examples**?
# Or give you **a practice program with all tuple methods in action**?
#  Or a **comparison of tuple vs list methods*