a = "deepli"
print(a[0:5])
print(a[2:])    
print(a[::-1])
print(a.count("e"))
print(a.replace("e","a"))
# Remember
# strip() → Removes spaces from both left and right.
# lstrip() → Removes spaces from the left only.
# rstrip() → Removes spaces from the right only

# Remove spaces from the right only
text = "Python   "
print(text.rstrip())